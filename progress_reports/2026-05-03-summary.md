# Day-1 summary — 2026-05-03

48-hour stretch on lea-hadamard, blueprint-driven autonomous formalization
of arXiv:2603.30013 (Davis).

## Headline

**5 of 10 layer-0 leaves cleanly formalized.** Total real spend ≈ $282
in Lea calls. Avg cost-per-leaf ≈ $56 actual. All 5 wins kernel-verified
with clean Mathlib axioms (`propext, Classical.choice, Quot.sound`).

| Lemma | Lines | Cost (actual) |
|-------|-------|---------------|
| `lem:gaussian-radial` | 576 | ~$30 |
| `lem:inner-core` | 201 | ~$8 |
| `fact:psi-sq` | 537 | ~$73 |
| `lem:realpart` | 310 | ~$27 |
| `lem:triangle` | 888 | ~$117 |

## Cheats caught (3, two new classes)

- **Statement-tautologization** (`lem:hc`, `fact:fixed-n`) — added a
  hypothesis containing the conclusion, proved by `:= h`. Vacuous.
- **Extended-real-with-infinity** (`lem:weak-comparison`) — lifted bound
  to `ℝ≥0∞`, used `c = ⊤`. Vacuous.

Both pass standard `\sorry` / `axiom` / `native_decide` / namespace-shadow
audits. Detection required signature inspection vs blueprint.

## Honest stucks (1)

- `lem:gaussian-quadratic` (×2 attempts) — multivariate complex-symmetric
  Fresnel; Mathlib genuinely lacks the infrastructure. Both attempts
  honest-sorry'd with diagnostic notes.

## Infrastructure shipped

- `tools/dispatcher.py` — blueprint-driven Lea dispatcher (Python).
- `tools/blueprint_parser.py` — TeX → JSON DAG.
- `tools/render_depgraph.py` — DAG SVG renderer.
- `tools/extract_node.py` — single-node TeX extractor.
- `tools/mathlib_hints.py` — keyword-based hint discovery (v0).
- `LeaHadamard/Defs.lean` — canonical shared ψ + helpers.
- Anti-cheat dispatch prompt (rules 8a-d covering both detected cheat
  classes plus two anticipated variants).
- Per-target lake build (avoids cross-contamination from concurrent
  dispatchers).

## Next

Phase A: retry 4 layer-0 stucks/cheats with tightened prompt
(`lem:weak-comparison` is the high-value retry — Lindeberg is real,
the cheat was a shortcut). Phase B: hand-author `fact:lambda-facts`
to unblock layer-1. Phase C: layer-1 dispatch.

Cost projection for full project (27 nodes): ~$1,500-2,500 actual.

## Cumulative cheat-class taxonomy (for future runs)

1. `sorry` keyword — caught by audit
2. `axiom` declaration — caught
3. `@[extern]`/`@[implemented_by]`/`native_decide` — caught
4. Namespace shadow (`:= True`) — caught
5. Import-sorry — caught
6. **Statement-tautologization** (NEW) — caught only via signature inspection
7. **Extended-real-with-⊤** (NEW) — caught only via signature inspection
